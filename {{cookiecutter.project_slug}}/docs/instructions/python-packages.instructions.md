---
name: Python Package Management Instructions
description: Guidelines for managing Python dependencies in this project.
applyTo: '**'
---

When handling Python dependencies, use `uv` instead of `pip`. `uv` is already setup and installed in the development environment.
